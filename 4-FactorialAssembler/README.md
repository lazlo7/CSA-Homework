# 4: Вычисление максимального аргумента факториала, который помещается в 64 бита

- factorial-max-arg.s: исходный код программы.

`$ make` для компиляции исходного кода.  
`$ ./factorial-max-arg` для запуска программы.